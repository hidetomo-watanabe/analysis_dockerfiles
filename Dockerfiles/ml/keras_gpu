# keras
ARG tensorflowVer=1.3.0
ARG tensorflowgpuVer=1.3.0
ARG kerasVer=2.0.8
RUN conda install -y tensorflow=${tensorflowVer} && \
  conda install -y tensorflow-gpu=${tensorflowgpuVer} && \
  conda install -y keras=${kerasVer}
